//import com.github.alantr7.bukkitplugin.beans.BeanReader;
import org.junit.Test;

import java.io.File;
import java.nio.file.Files;

public class ReaderTest {

    @Test
    public void test() throws Exception {/*
        var classes = BeanReader.getClasses(Files.readAllBytes(new File(
                "D:/Workspace/java/.out/DerMagic/classes/beans.dat"
        ).toPath()));
        System.out.println("Loaded " + classes.length + " classes.");*/
    }

}
